import Vue from 'vue';
import Router from 'vue-router';
import Payment from './views/Payment';

Vue.use(Router);

export default new Router({
  mode: 'history',
  routes: [
    {
      path: '/payment',
      name: 'payment',
      component: Payment
    }
  ]
});
