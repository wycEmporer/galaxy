import Vue from 'vue';
import Router from 'vue-router';
import Trip from './views/Trip';

Vue.use(Router);
export default new Router({
  mode: 'history',
  routes: [
    {
      path: '/trip',
      name: 'trip',
      component: Trip
    }
  ]
});
